---
// import { headerNav } from "@data/navigation";
---

<header class="site-header">
    <nav aria-label="Primary" class="cluster">
        <!-- {headerNav.map((item) => <a href={item.href}>{item.title}</a>)} -->
    </nav>
</header>
